{
  "name": "unreal-assistant",
  "version": "1.0.0",
  "description": "Unreal - AI Voice Assistant",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Static app - no build needed'",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
